-- 코드를 입력하세요
SELECT ANIMAL_ID, NAME
FROM (
    SELECT distinct i.DATETIME, i.ANIMAL_ID, i.NAME
    FROM ANIMAL_INS i, ANIMAL_OUTS o
    WHERE
        i.ANIMAL_ID = o.ANIMAL_ID
        and i.DATETIME >= o.DATETIME
    ORDER BY i.DATETIME
)

    